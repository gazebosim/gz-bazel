# -*- python -*-

load(":vars.bzl", "LIBDIR")

cc_library(
    name = "zip",
    hdrs = glob([
        "include/zip.h",
    ]),
    includes = ["include"],
    linkopts = [
        "-L" + LIBDIR,
        "-Wl,-rpath," + LIBDIR,
        "-lzip",
    ],
    visibility = ["//visibility:public"],
)
